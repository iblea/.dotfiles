# Catppuccin 테마
set -g @catppuccin_flavor "mocha"
set -g @catppuccin_window_status_style "basic"

# Nerd Font 없는 환경 호환: 아이콘/separator 제거
set -g @catppuccin_application_icon ""
set -g @catppuccin_status_left_separator ""
set -g @catppuccin_status_middle_separator " "
set -g @catppuccin_status_right_separator ""

# 윈도우 탭 (왼쪽): Claude Code이면 spinner + "Claude Code", 아니면 pane_current_command
set -g @catppuccin_window_text " #{?#{||:#{@is_claude_code},#{m:*Claude Code*,#{pane_title}}},#{s/ .*/ Claude Code/:pane_title},#{pane_current_command}}"
set -g @catppuccin_window_current_text " #{?#{||:#{@is_claude_code},#{m:*Claude Code*,#{pane_title}}},#{s/ .*/ Claude Code/:pane_title},#{pane_current_command}}"

# application 모듈 (오른쪽): Claude Code이면 실제 pane_title(작업상태), 아니면 pane_current_command
set -g @catppuccin_application_text " #{?#{||:#{@is_claude_code},#{m:*Claude Code*,#{pane_title}}},#{pane_title},#{pane_current_command}}"

# 윈도우 번호: pane 2개 이상이면 "윈도우.pane인덱스" 형식
set -g @catppuccin_window_number "#{?#{e|>:#{window_panes},1},#I.#{pane_index},#I}"
set -g @catppuccin_window_current_number "#{?#{e|>:#{window_panes},1},#I.#{pane_index},#I}"

# 플러그인 로드
run ~/.config/tmux/catppuccin/catppuccin.tmux

# 상태바 구성
set -g status-position top
set -g status-right-length 100
set -g status-left ""
set -g status-right "#{?pane_in_mode,#[bg=#f38ba8 fg=#11111b bold] COPY #[default] ,}#{E:@catppuccin_status_application}"

# copy mode 시 상태바 배경색 변경
set -g status-style "#{?pane_in_mode,bg=#45475a,bg=default}"
